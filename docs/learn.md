1.创建品牌和更新品牌多对多，还有层级（树形结构可作为参考学习，重写这一块之类的）

这个进程是我在会话后台起的,会话结束后会停。之后你自己想用时,在终端跑 npx redis-commander 即可(端口 8081)。
它默认监听 0.0.0.0,局域网内其他设备也能访问,如果你只在本机用,可以加参数 --http-port 8081 --http-host 127.0.0.1 收窄到本机。
以后如果网络能直连外网了,随时可以 brew install --cask redisinsight 装官方桌面版,界面更好用。